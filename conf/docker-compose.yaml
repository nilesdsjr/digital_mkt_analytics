version: '3'

services:
  teste-postgres-compose:
    image: postgres
    environment:
      POSTGRES_PASSWORD: "Postgres"
    ports:
      - "15432:5432"
 
    networks:
      - postgres-compose-network

networks: 
  postgres-compose-network:
    driver: bridge